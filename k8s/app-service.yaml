apiVersion: v1
kind: Service
metadata:
  name: portuguese-learning-app
  namespace: portuguese-learning
spec:
  selector:
    app: portuguese-learning-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
